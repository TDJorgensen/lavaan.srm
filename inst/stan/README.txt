TODO:

- replace all instances of "person" or "p" with "case" and "c"
  - in .stan and in .R files
  - replace AP with OI (out-in)?
- explain my naming conventions in this README file
- change srm_priors() to srmPriors()
- update priors for case-covariate SDs to match srm_priors()
